{
    "name": "exolnet/wp-graylog",
    "description": "Adds a mu-plugin that register a Monolog handler to send exception to a Graylog channel.",
    "type": "wordpress-muplugin",
    "keywords": [
        "exolnet",
        "wordpress",
        "monolog",
        "graylog"
    ],
    "homepage": "https://github.com/eXolnet/wp-graylog",
    "license": "MIT",
    "authors": [
        {
            "name": "eXolnet",
            "homepage": "https://www.exolnet.com"
        }
    ],
    "require": {
        "php": "^7.1",
        "filp/whoops": "^2.3",
        "graylog2/gelf-php": "^1.6",
        "monolog/monolog": "^1.24"
    },
    "require-dev": {
        "squizlabs/php_codesniffer": "^3.4"
    },
    "scripts": {
        "lint": "vendor/bin/phpcs -p --standard=PSR2 --extensions=php --ignore=*/vendor/* .",
        "lint-fix": "vendor/bin/phpcbf -p --standard=PSR2 --extensions=php --ignore=*/vendor/* ."
    },
    "config": {
        "sort-packages": true
    }
}
